require 'spec_helper'

describe Zenhub2pivotal do
  it 'has a version number' do
    expect(Zenhub2pivotal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
